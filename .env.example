# Copia este archivo a .env.local (no se sube a git) y pega la llave real.
# Llave compartida de Groq con la que arrancan todas las cuentas nuevas. Empieza con gsk_
# Se crea en https://console.groq.com/keys
VITE_GROQ_KEY=
